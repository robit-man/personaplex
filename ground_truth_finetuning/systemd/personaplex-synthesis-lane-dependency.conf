[Unit]
# Supersede the legacy shared Voicebox service. Each synthesis lane owns the
# matching CUDA-pinned worker so lifecycle and memory accounting stay aligned.
Requires=
After=
Requires=personaplex-v7-voicebox-worker@%i.service
After=personaplex-v7-voicebox-worker@%i.service
