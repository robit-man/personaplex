import { AbstractSession } from '@helia/utils';
import type { BitswapWantProgressEvents } from './index.ts';
import type { Network } from './network.ts';
import type { WantList } from './want-list.ts';
import type { BlockRetrievalOptions, CreateSessionOptions } from '@helia/interface';
import type { ComponentLogger, Libp2p, PeerId } from '@libp2p/interface';
import type { Multiaddr } from '@multiformats/multiaddr';
import type { AbortOptions } from 'interface-store';
import type { CID } from 'multiformats/cid';
export interface BitswapSessionComponents {
    network: Network;
    wantList: WantList;
    logger: ComponentLogger;
    libp2p: Libp2p;
}
interface ProviderPeer {
    peerId: PeerId;
    routing: string;
    toString(): string;
}
declare class BitswapSession extends AbstractSession<ProviderPeer, BitswapWantProgressEvents> {
    readonly name = "bitswap-session";
    private readonly wantList;
    private readonly network;
    private readonly libp2p;
    constructor(components: BitswapSessionComponents, init: CreateSessionOptions);
    queryProvider(cid: CID, provider: ProviderPeer, options: AbortOptions): Promise<Uint8Array>;
    findNewProviders(cid: CID, options?: AbortOptions): AsyncGenerator<ProviderPeer>;
    toFilterKey(provider: ProviderPeer): Uint8Array | string;
    equals(providerA: ProviderPeer, providerB: ProviderPeer): boolean;
    convertToProvider(provider: PeerId | Multiaddr | Multiaddr[], routing: string, options?: AbortOptions): Promise<ProviderPeer | undefined>;
    emitFoundProviderProgressEvent(cid: CID, provider: ProviderPeer, options: BlockRetrievalOptions<BitswapWantProgressEvents>): void;
}
export declare function createBitswapSession(components: BitswapSessionComponents, init: CreateSessionOptions): BitswapSession;
export {};
//# sourceMappingURL=session.d.ts.map