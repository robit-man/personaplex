import { CID } from 'multiformats/cid';
import type { AddOptions, GetOptions, StringsComponents, Strings as StringsInterface } from './index.js';
export declare class Strings implements StringsInterface {
    private readonly components;
    constructor(components: StringsComponents);
    add(string: string, options?: AddOptions): Promise<CID>;
    get(cid: CID, options?: GetOptions): Promise<string>;
}
//# sourceMappingURL=strings.d.ts.map