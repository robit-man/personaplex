import { contentRoutingSymbol, peerRoutingSymbol } from '@libp2p/interface';
import { CID } from 'multiformats/cid';
import type { DelegatedRoutingV1HttpApiClient as DelegatedRoutingV1HttpApiClientInterface, DelegatedRoutingV1HttpApiClientInit, GetProvidersOptions, GetPeersOptions, GetIPNSOptions, PeerRecord, DelegatedRoutingV1HttpApiClientComponents, GetClosestPeersOptions } from './index.js';
import type { ContentRouting, PeerRouting, AbortOptions, PeerId } from '@libp2p/interface';
import type { IPNSRecord } from 'ipns';
export declare class DelegatedRoutingV1HttpApiClient implements DelegatedRoutingV1HttpApiClientInterface {
    #private;
    readonly url: URL;
    private started;
    private readonly httpQueue;
    private readonly shutDownController;
    private readonly timeout;
    private readonly contentRouting;
    private readonly peerRouting;
    private readonly filterAddrs?;
    private readonly filterProtocols?;
    private readonly inFlightRequests;
    private readonly cacheName;
    private cache?;
    private readonly cacheTTL;
    private log;
    /**
     * Create a new DelegatedContentRouting instance
     */
    constructor(components: DelegatedRoutingV1HttpApiClientComponents, init: DelegatedRoutingV1HttpApiClientInit & {
        url: string | URL;
    });
    get [contentRoutingSymbol](): ContentRouting;
    get [peerRoutingSymbol](): PeerRouting;
    isStarted(): boolean;
    start(): Promise<void>;
    stop(): Promise<void>;
    getProviders(cid: CID, options?: GetProvidersOptions): AsyncGenerator<PeerRecord>;
    getPeers(peerId: PeerId, options?: GetPeersOptions): AsyncGenerator<PeerRecord>;
    getClosestPeers(key: CID | PeerId, options?: GetClosestPeersOptions): AsyncGenerator<PeerRecord>;
    getIPNS(libp2pKey: CID<unknown, 0x72, 0x00 | 0x12, 1>, options?: GetIPNSOptions): Promise<IPNSRecord>;
    putIPNS(libp2pKey: CID<unknown, 0x72, 0x00 | 0x12, 1>, record: IPNSRecord, options?: AbortOptions): Promise<void>;
    toString(): string;
    private logRequest;
    private logResponse;
}
//# sourceMappingURL=client.d.ts.map