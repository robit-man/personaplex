import { BaseBlockstore } from './base.js';
import type { Blockstore, InputPair, Pair } from 'interface-blockstore';
import type { AbortOptions, AwaitGenerator, AwaitIterable } from 'interface-store';
import type { CID } from 'multiformats/cid';
/**
 * A blockstore that can combine multiple stores. Puts and deletes
 * will write through to all blockstores. Has and get will
 * try each store sequentially. getAll will use every store but also
 * deduplicate any yielded pairs.
 */
export declare class TieredBlockstore extends BaseBlockstore {
    private readonly stores;
    constructor(stores: Blockstore[]);
    put(key: CID, value: Uint8Array | AwaitIterable<Uint8Array>, options?: AbortOptions): Promise<CID>;
    get(key: CID, options?: AbortOptions): AwaitGenerator<Uint8Array>;
    has(key: CID, options?: AbortOptions): Promise<boolean>;
    delete(key: CID, options?: AbortOptions): Promise<void>;
    putMany(source: AwaitIterable<InputPair>, options?: AbortOptions): AwaitGenerator<CID>;
    deleteMany(source: AwaitIterable<CID>, options?: AbortOptions): AwaitGenerator<CID>;
    getAll(options?: AbortOptions): AwaitGenerator<Pair>;
}
//# sourceMappingURL=tiered.d.ts.map