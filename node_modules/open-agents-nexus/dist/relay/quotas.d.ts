export interface RelayReservation {
    peerId: string;
    grantedAt: number;
    expiresAt: number;
    maxStreams: number;
    maxBytes: number;
    usedBytes: number;
    activeStreams: number;
}
export interface RelayQuotaConfig {
    maxReservationsPerPeer: number;
    maxTotalReservations: number;
    defaultDurationSec: number;
    maxDurationSec: number;
    defaultMaxStreams: number;
    defaultMaxBytes: number;
}
export declare const DEFAULT_RELAY_QUOTA: RelayQuotaConfig;
export declare class RelayQuotaManager {
    private reservations;
    private config;
    constructor(config?: Partial<RelayQuotaConfig>);
    reserve(peerId: string, requestedDurationSec?: number, requestedMaxStreams?: number, requestedMaxBytes?: number): RelayReservation | null;
    hasReservation(peerId: string): boolean;
    recordBytes(peerId: string, bytes: number): boolean;
    getStats(): {
        totalReservations: number;
        peersWithReservations: number;
    };
    private cleanup;
}
