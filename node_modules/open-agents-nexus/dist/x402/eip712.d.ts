/**
 * EIP-712 Typed Data for USDC Transfers (EIP-3009)
 *
 * Implements the `transferWithAuthorization` signing and verification
 * for USDC on Base (Chain ID 8453). Uses viem for all cryptographic
 * operations — no external signing services.
 */
import type { PrivateKeyAccount } from 'viem/accounts';
/** USDC contract address on Base mainnet */
export declare const USDC_BASE_ADDRESS: "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913";
/** Base chain ID */
export declare const BASE_CHAIN_ID = 8453;
/** EIP-712 domain separator for USDC on Base */
export declare const USDC_EIP712_DOMAIN: {
    readonly name: "USD Coin";
    readonly version: "2";
    readonly chainId: 8453;
    readonly verifyingContract: "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913";
};
/** EIP-3009 TransferWithAuthorization typed data */
export declare const TRANSFER_WITH_AUTH_TYPES: {
    readonly TransferWithAuthorization: readonly [{
        readonly name: "from";
        readonly type: "address";
    }, {
        readonly name: "to";
        readonly type: "address";
    }, {
        readonly name: "value";
        readonly type: "uint256";
    }, {
        readonly name: "validAfter";
        readonly type: "uint256";
    }, {
        readonly name: "validBefore";
        readonly type: "uint256";
    }, {
        readonly name: "nonce";
        readonly type: "bytes32";
    }];
};
/** The message payload for a TransferWithAuthorization signature */
export interface TransferAuthMessage {
    from: `0x${string}`;
    to: `0x${string}`;
    value: bigint;
    validAfter: bigint;
    validBefore: bigint;
    nonce: `0x${string}`;
}
/**
 * Sign a TransferWithAuthorization message using the agent's wallet.
 * Returns an EIP-712 signature (0x-prefixed hex).
 */
export declare function signPaymentAuthorization(account: PrivateKeyAccount, message: TransferAuthMessage): Promise<`0x${string}`>;
/**
 * Verify an EIP-712 signature against a claimed signer address.
 * Pure crypto — no network calls needed.
 */
export declare function verifyPaymentSignature(address: `0x${string}`, message: TransferAuthMessage, signature: `0x${string}`): Promise<boolean>;
/**
 * Generate a random bytes32 nonce for EIP-3009 authorization.
 * Each authorization must use a unique nonce — the contract rejects replays.
 */
export declare function generateNonce(): `0x${string}`;
