import type { PrivateKey } from '@libp2p/interface';
export interface BatchMessage {
    id: string;
    timestamp: number;
    sender: string;
    format: string;
    content: string;
}
export interface MessageBatch {
    schema: 'nexus:message-batch:v1';
    roomId: string;
    batchId: string;
    author: string;
    createdAt: number;
    messages: BatchMessage[];
    retentionClass: 'ephemeral' | 'cache' | 'retained' | 'mirrored' | 'archival';
    sig: string;
}
export declare const MAX_BATCH_SIZE = 100;
export declare function createBatch(roomId: string, author: string, messages: BatchMessage[], retentionClass?: MessageBatch['retentionClass']): Omit<MessageBatch, 'sig'>;
export declare function signBatch(batch: Omit<MessageBatch, 'sig'>, privateKey: PrivateKey): Promise<MessageBatch>;
export declare function validateBatchStructure(batch: unknown): batch is MessageBatch;
