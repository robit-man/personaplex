export type RetentionClass = 'ephemeral' | 'cache' | 'retained' | 'mirrored' | 'archival';
export interface RetentionConfig {
    maxTotalBytes: number;
    maxPerRoomBytes: number;
    ttlByClass: Record<RetentionClass, number>;
    allowedMirrorRooms: string[];
    gcIntervalMs: number;
}
export declare const DEFAULT_RETENTION: RetentionConfig;
export interface StoredObject {
    cid: string;
    roomId?: string;
    retentionClass: RetentionClass;
    storedAt: number;
    sizeBytes: number;
}
export declare class RetentionPolicyEngine {
    private config;
    private objects;
    constructor(config?: Partial<RetentionConfig>);
    shouldAccept(obj: StoredObject): {
        accept: boolean;
        reason?: string;
    };
    track(obj: StoredObject): void;
    untrack(cid: string): void;
    getExpired(): string[];
    gc(): string[];
    getTotalSize(): number;
    getRoomSize(roomId: string): number;
    getTrackedCount(): number;
}
