import type { TrustPolicy } from './policy.js';
export interface TrustConfig {
    allowlist?: string[];
    denylist?: string[];
    maxUnknownPeers?: number;
    roomDenylist?: string[];
}
export declare class DefaultTrustPolicy implements TrustPolicy {
    private allowSet;
    private denySet;
    private roomDenySet;
    constructor(config?: TrustConfig);
    allowPeer(peerId: string): boolean;
    allowRoom(roomId: string, peerId: string): boolean;
    allowCapability(peerId: string, capability: string): boolean;
    allowRelay(peerId: string): boolean;
    addToAllowlist(peerId: string): void;
    removeFromAllowlist(peerId: string): void;
    addToDenylist(peerId: string): void;
    removeFromDenylist(peerId: string): void;
    denyRoom(roomId: string): void;
    allowRoomId(roomId: string): void;
}
