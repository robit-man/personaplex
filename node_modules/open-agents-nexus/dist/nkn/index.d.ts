export interface NknConfig {
    enabled: boolean;
    identifier: string;
    numSubClients: number;
}
export declare const DEFAULT_NKN_CONFIG: NknConfig;
export interface NknAddressInfo {
    address: string;
    publicKey: string;
    seed: string;
}
export declare class NknFallback {
    private client;
    private config;
    private addressInfo;
    private onMessage;
    constructor(config?: Partial<NknConfig>);
    connect(seed?: string): Promise<NknAddressInfo | null>;
    send(to: string, message: string): Promise<boolean>;
    setMessageHandler(handler: (src: string, payload: string) => void): void;
    disconnect(): Promise<void>;
    get address(): string | null;
    get isConnected(): boolean;
}
