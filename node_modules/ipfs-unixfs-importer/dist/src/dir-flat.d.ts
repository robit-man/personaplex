import { Dir } from './dir.ts';
import type { DirProps } from './dir.ts';
import type { ImportResult, InProgressImportResult, WritableStorage } from './index.ts';
import type { AddToTreeOptions } from './tree-builder.ts';
export declare class DirFlat extends Dir {
    private readonly _children;
    constructor(props: DirProps, options: AddToTreeOptions);
    put(name: string, value: InProgressImportResult | Dir): Promise<void>;
    get(name: string): Promise<InProgressImportResult | Dir | undefined>;
    childCount(): number;
    directChildrenCount(): number;
    onlyChild(): InProgressImportResult | Dir | undefined;
    eachChildSeries(): Generator<{
        key: string;
        child: InProgressImportResult | Dir;
    }, void, undefined>;
    marshal(): Uint8Array;
    estimateNodeSize(): Promise<number>;
    flush(block: WritableStorage): AsyncGenerator<ImportResult>;
}
//# sourceMappingURL=dir-flat.d.ts.map