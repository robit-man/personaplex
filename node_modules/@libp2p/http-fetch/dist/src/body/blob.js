export function blobBody(blob, headers) {
    headers.set('Content-Length', `${blob.size}`);
    headers.set('Content-Type', (blob.type != null && blob.type !== '') ? blob.type : 'application/octet-stream');
    return blob.stream();
}
//# sourceMappingURL=blob.js.map