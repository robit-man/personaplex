export async function prepareAndSendRequest(resource, opts, sendRequest) {
    for (const middleware of opts.middleware) {
        await middleware.prepareRequest?.(resource, opts);
    }
    return sendRequest();
}
export async function prepareAndConnect(resource, opts, connect) {
    for (const middleware of opts.middleware) {
        await middleware.prepareRequest?.(resource, opts);
    }
    return connect();
}
export async function processResponse(resource, opts, response) {
    for (const middleware of opts.middleware) {
        await middleware.processResponse?.(resource, opts, response);
    }
    return response;
}
//# sourceMappingURL=utils.js.map