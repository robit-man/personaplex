import type { Middleware, MiddlewareOptions } from '@libp2p/http-utils';
import type { ComponentLogger } from '@libp2p/interface';
import type { Multiaddr } from '@multiformats/multiaddr';
interface CookiesComponents {
    logger: ComponentLogger;
}
export interface CookiesInit {
    cookieExpiryCheckInterval?: number;
}
export declare class Cookies implements Middleware {
    private readonly log;
    private readonly cookies;
    constructor(components: CookiesComponents, init?: CookiesInit);
    prepareRequest(resource: URL | Multiaddr[], opts: MiddlewareOptions): Promise<void>;
    processResponse(resource: URL | Multiaddr[], opts: MiddlewareOptions, response: Response): Promise<void>;
}
export {};
//# sourceMappingURL=cookies.d.ts.map