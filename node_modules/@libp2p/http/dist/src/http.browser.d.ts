import { serviceCapabilities } from '@libp2p/interface';
import type { HTTPInit, HTTP as HTTPInterface, ProtocolMap, FetchInit, HTTPRoute, ConnectInit } from './index.js';
import type { ComponentLogger, PeerId, PrivateKey, Startable, AbortOptions } from '@libp2p/interface';
import type { ConnectionManager, Registrar } from '@libp2p/interface-internal';
import type { Multiaddr } from '@multiformats/multiaddr';
export interface HTTPComponents {
    privateKey: PrivateKey;
    registrar: Registrar;
    connectionManager: ConnectionManager;
    logger: ComponentLogger;
}
export declare class HTTP implements HTTPInterface, Startable {
    private readonly log;
    protected readonly components: HTTPComponents;
    private readonly httpRegistrar;
    private readonly origin;
    private readonly cookies;
    constructor(components: HTTPComponents, init?: HTTPInit);
    readonly [Symbol.toStringTag] = "@libp2p/http";
    readonly [serviceCapabilities]: string[];
    start(): Promise<void>;
    stop(): Promise<void>;
    agent(...args: any[]): any;
    dispatcher(...args: any[]): any;
    connect(resource: string | URL | PeerId | Multiaddr | Multiaddr[], init?: ConnectInit): Promise<globalThis.WebSocket>;
    fetch(resource: string | URL | PeerId | Multiaddr | Multiaddr[], init?: FetchInit): Promise<Response>;
    connectProtocol(resource: string | URL | PeerId | Multiaddr | Multiaddr[], protocol: string, init?: ConnectInit): Promise<globalThis.WebSocket>;
    fetchProtocol(resource: string | URL | PeerId | Multiaddr | Multiaddr[], protocol: string, init?: FetchInit): Promise<Response>;
    getSupportedProtocols(resource: string | URL | PeerId | Multiaddr | Multiaddr[], options?: AbortOptions): Promise<ProtocolMap>;
    getProtocolPath(peer: string | URL | PeerId | Multiaddr | Multiaddr[], protocol: string, options?: AbortOptions): Promise<string>;
    canHandle(req: {
        url?: string;
    }): boolean;
    onRequest(req: Request): Promise<Response>;
    onWebSocket(ws: globalThis.WebSocket): void;
    handle(protocol: string, handler: HTTPRoute): void;
    unhandle(protocol: string): void;
    getProtocolMap(): ProtocolMap;
    private sendRequest;
}
//# sourceMappingURL=http.browser.d.ts.map