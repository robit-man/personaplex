import toBuffer from 'it-to-buffer';
import { rawContent } from "./utils/raw-content.js";
export async function rawResolver(cid, name, path, blockstore, options) {
    const block = await toBuffer(blockstore.get(cid, options));
    return {
        type: 'raw',
        cid,
        name,
        path,
        content: rawContent(block, 'unixfs:exporter:progress:raw'),
        size: BigInt(block.length),
        node: block
    };
}
//# sourceMappingURL=raw.js.map