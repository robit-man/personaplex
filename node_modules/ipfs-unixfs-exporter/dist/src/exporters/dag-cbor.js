import * as dagCbor from '@ipld/dag-cbor';
import toBuffer from 'it-to-buffer';
export async function dagCborResolver(cid, name, path, blockstore, options) {
    const block = await toBuffer(blockstore.get(cid, options));
    const object = dagCbor.decode(block);
    return {
        type: 'object',
        cid,
        name,
        path,
        object,
        node: block
    };
}
//# sourceMappingURL=dag-cbor.js.map