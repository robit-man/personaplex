import { Bucket } from 'hamt-sharding';
import type { ReadableStorage, WalkPathOptions } from '../../index.js';
import type { ResolveResult } from '../index.ts';
import type { PBNode } from '@ipld/dag-pb';
interface ShardTraversalContext {
    hamtDepth: number;
    rootBucket: Bucket<boolean>;
    lastBucket: Bucket<boolean>;
}
export declare function findShardCid(node: PBNode, name: string, rest: string[], blockstore: ReadableStorage, context?: ShardTraversalContext, options?: WalkPathOptions): AsyncGenerator<ResolveResult>;
export {};
//# sourceMappingURL=find-cid-in-shard.d.ts.map